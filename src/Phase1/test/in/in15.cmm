/* this is
    just ///////
    beginning of hard
    examples!!!!! :))
*/

struct s1__
    list# list# int myvar (fptr<void -> bool> arg1, int arg2) begin
        set begin
            a = b = c * d = v * f + c; display(arg1 > hdfn.jji* jl);
            int a = n * d + c , b = u / d
        end
        get



            return size(jj.l.d)
    end
struct s2 begin
    int a = size(append(size(l() + f().f * f), true));
    list #list #list #struct k t = s1.ff; struct temp tp = s2.d.ll.oi() == l / 3



    int newVar () begin
            set
                display(ki & r)


            get
                return mr.e.w;
    end; int d() begin
            set
                a = a == l * r < df


            get begin
                display(f()); display(size(f().d[k.r]))




                return sld /* commeeeeeeent
                */
            end
    end; bool t


 end

 /* another
    comment */
list #list #list #list #struct inti jlist(fptr<void -> struct t> arg1, struct f h)


    append(arg1.j, f.l.p[true * -2 * -re])
void func2() begin
    display(whatever / ~jf)

end
int lastFunc() begin
    a = a = c*d = c +d = c -d = c/d; a = (c *d = -sda)
end





 main()
    return lastFunc()()()().svd + lw * nf - st.f.g[o | b * r & a];









